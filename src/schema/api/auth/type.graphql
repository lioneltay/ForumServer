extend type Query {
  currentUser: User
}

extend type Mutation {
  signup(
    firstName: String!
    lastName: String!
    email: String!
    password: String!
  ): User

  login(email: String!, password: String!): User

  logout: User
}
