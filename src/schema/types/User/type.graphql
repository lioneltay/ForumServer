enum FollowType {
  ALL
  FOLLOWER
  FOLLOWEE
}

type User {
  id: ID
  firstName: String
  lastName: String
  fullName: String
  email: String
  createdAt: String
  comments: [Comment]
  threads: [Thread]
  follows(type: FollowType): [Follow]
  followers: [User]
  followees: [User]
}
